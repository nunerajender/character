<!DOCTYPE html>

<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if IE 8]>    <html class="no-js lt-ie9" lang="en"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js" lang="en"> <!--<![endif]-->

<head>
  <meta charset='utf-8' />

  <meta name='viewport' content='width=device-width, initial-scale=1.0' />

  <title><%= Character.title %> | Admin</title>

  <%= stylesheet_link_tag 'character', :media => 'all' %>

  <%= javascript_include_tag 'vendor/custom.modernizr' %>

  <%= javascript_include_tag 'character' %>
  <%= setup_browserid %>

  <% if @admin_user %>
    <script type='text/javascript'>
      window.user_image_url = '<%= @admin_user.gravatar_url(36) %>';      
      $(function(){ character.start() });
    </script>

    <style>
    <% if Character.company_logo_image.start_with?('http://') %>
      .chr-generic-details { background-image: url(<%= Character.company_logo_image %>); }
    <% else %>
      .chr-generic-details { background-image: url(<%= image_path(Character.company_logo_image) %>); }
    <% end %>
    </style>

    <%= csrf_meta_tags %>
  <% else %>
    <style>
      <% if Character.login_background_image.start_with?('http://') %>
      .chr-login { background-image:url("<%= Character.login_background_image %>"); }
      <% else %>
      .chr-login { background-image:url("<%= image_path(Character.login_background_image) %>"); }
      <% end %>
    </style>
  <% end %>
</head>

<body class='antialiased'>
  <% unless @admin_user %>

    <div class='chr-login'>  <!-- NICE BG: http://feeds.nationalgeographic.com/ng/photography/photo-of-the-day/ -->
    
      <%= link_to browserid_config.login.path, class: 'browserid_login button' do %>
        <i class='icon-user' style='margin-right:.3em;'></i>  Login with Persona
      <% end %>

    </div>
  <% end %>
</body>
</html>
